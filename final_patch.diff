diff --git a/test.c b/test.c
index 95cc8e0..61c434e 100644
--- a/test.c
+++ b/test.c
@@ -7,11 +7,19 @@ void bad() {
     int arr[n];
     arr[1] = 2;
     arr[0] = 3;
-    printf("%d %d %d", arr[0], arr[1], arr[2]);
+    // Fixing potential out-of-bounds access
+    if (n > 2) {
+        printf("%d %d %d", arr[0], arr[1], arr[2]);
+    } else {
+        printf("%d %d", arr[0], arr[1]);
+    }
     int val = 0;
-    val = arr[x+y+5];
+    // Fixing potential out-of-bounds access
+    if (x + y + 5 < n) {
+        val = arr[x + y + 5];
+    }
     printf("testing\n");
-    return
+    return;
 }
 
 int main() {
